export enum BaseStatusEnum {
    Passive,
    Active
}

export enum MfaEnum {
    Email,
    GoogleAuth
}

export enum MfaStatusEnum {
    NotUsed,
    Correct,
    Wrong
}

export enum PasswordChangeEnum {
    Change,
    Forgetten
}